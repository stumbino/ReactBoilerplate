import React from 'react';
import ReactDOM from 'react-dom';

var Main = React.createClass({
      render(){
        return(
          <div>
          "hello World"
          </div>
        )
      }
});

module.export = Main;
